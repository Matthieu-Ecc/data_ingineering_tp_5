pipeline {
    agent any 
    stages {
        stage('Compile') {
            steps {
                echo 'Compile the source code' 
                Run 'docker-compose up -d --build'
            }
        }
        stage('Run') {
            steps {
                echo 'Run the App'
                Run 'docker-compose up'
            }
        }
    }
}