pipeline {
    agent any 
    stages {
        stage('Compile') {
            steps {
                echo 'Compile the source code' 
                bat 'docker-compose up -d --build'
            }
        }
        stage('Run') {
            steps {
                echo 'Run the App'
                bat 'docker-compose up'
            }
        }
    }
}